import React, { Component } from 'react';
import styles from './style.css';

class Header extends Component {
  render() {
    return (
      <h1 className={styles.main}>Play the Powerball!</h1>
    );
  }
}

export default Header;
